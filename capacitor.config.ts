import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.calculadoravintage',
  appName: 'Calculator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
